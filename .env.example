# ══════════════════════════════════════════════════════════════════
#  Panimii Bakery — Variables de Entorno
#  Copia este archivo como .env y rellena los valores reales.
#  NUNCA subas el archivo .env real a GitHub.
# ══════════════════════════════════════════════════════════════════

# ── Django ──────────────────────────────────────────────────────
# Genera una clave con: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=tu-clave-secreta-aqui

# True para desarrollo local, False en producción
DEBUG=True

# Hosts permitidos separados por coma (ej: localhost,127.0.0.1,tupaniimidominio.com)
ALLOWED_HOSTS=localhost,127.0.0.1

# ── Base de Datos (MySQL en producción) ─────────────────────────
DB_ENGINE=django.db.backends.sqlite3
DB_NAME=db.sqlite3
# Descomentar para MySQL:
# DB_ENGINE=django.db.backends.mysql
# DB_NAME=panimii_db
# DB_USER=tu_usuario_mysql
# DB_PASSWORD=tu_password_mysql
# DB_HOST=localhost
# DB_PORT=3306

# ── Correo Electrónico (SMTP) ───────────────────────────────────
# Para Hosting Compartido (cPanel) con correo propio:
EMAIL_HOST=mail.paniimibakery.com
EMAIL_PORT=465
EMAIL_USE_TLS=False
EMAIL_USE_SSL=True
EMAIL_HOST_USER=no-reply@paniimibakery.com
EMAIL_HOST_PASSWORD=tu_contrasena_de_correo

# Correo destino donde llegan los mensajes del formulario
EMAIL_DESTINATARIO=paniimib@gmail.com

# ── Stripe (Pagos) ──────────────────────────────────────────────
# Obtén tus claves en https://dashboard.stripe.com/test/apikeys
# Usa las claves de PRUEBA (pk_test_ / sk_test_) para desarrollo
STRIPE_PUBLIC_KEY=pk_test_...
STRIPE_SECRET_KEY=sk_test_...
